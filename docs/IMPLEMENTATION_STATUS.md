# First release implementation status

Updated 13 September 2026. This is the coming-soon release and first local editor prototype, not the full membership website.

## Implemented

- Responsive, static Astro coming-soon page using the supplied wordmark, pale blue, Literata and Golos Text.
- Historical Eugene Onegin garden illustration by Elena Samokish-Sudkovskaya (1908), with source credit, descriptive alternative text and responsive WebP images. The editor can hide it; changing the first book removes this specific artwork. See [artwork provenance](ARTWORK.md).
- Five faint background book imprints use three transparent illustrations optimized as WebP masks. They follow the page colours, stay outside editable text and links, and adapt to available space. Layout & style includes a Background book drawings switch. See [assets and generation prompts](BOOK_IMPRINTS.md).
- The masthead description says “with Olga” and ends with a linked (@books_olgaruchina), using an optional Instagram profile field in the editor. There is no separate founder section; the club’s @ruslitiki handle is separately linked in the footer, opening a new tab. Other public copy retains Olga Ruchina’s full name; the AI overview follows the current page content.
- Confirmed waitlist link, membership opening 25 September and reading start 1 October, first read *Eugene Onegin*, optional working email and future Patreon states. The two dates stack in larger type on the left below the signup button, with Membership and its editable $15 USD/month price on the right. Narrow screens place the price below the dates; early-bird pricing stays in the Membership section and FAQ.
- Separate “How the club works?” and Membership sections explain the chosen book, weekly video/guide/materials, reading guidance, monthly guided discussion without preparation, and advance calendar. Confirmed pricing is $15 USD per month, or $9.99 USD per month for early birds.
- The FAQ introduction retains its heading without filler text. Text blocks can publish a heading alone; the editor keeps their optional text editable, and FAQ answers remain required.
- Seven expandable FAQs explain the audience, translation, weekly offering, launch, discussion preparation, timezones and waitlist/membership distinction. Early-bird eligibility/duration, exact session times and replay arrangements remain unconfirmed.
- Editable YouTube blocks accept video links and produce responsive, lazy-loaded privacy-enhanced players with no autoplay. The supplied club introduction (`m9CKv9oMYRY`) is visible after the opening, where the single How it works menu link starts the video and written explanation group. It falls back to the written section if the intro is hidden or moved away; explicit menu labels remain configurable. The centered 16:9 player has an indigo border and a 56rem maximum width (896 × 504 pixels at the default text size) and fits the available width on smaller screens. Player interaction is reserved for Saved preview; the editing canvas opens video settings.
- Privacy page, 404, canonical metadata, sitemap, robots, organization data, accessible headings and controls; a small public script for the mobile drawer, with no editor runtime or analytics.
- Generated `/llms.txt` with the club overview, dates, first book, visible section copy, active join/contact links and privacy link. It uses the same reviewed content snapshot as the HTML, omits hidden and inactive fields, follows block order and escapes plain editor text as Markdown. Pages include a discovery link.
- The wordmark is 25% smaller at every viewport width; a horizontal rule introduces the video-and-explanation group, with no extra rule between those adjacent sections. Outbound, artwork-credit and privacy links open in a new tab with noopener. Home and section links keep visitors in the current tab.
- The logo has modest breathing room above and below it. Opening top padding is three quarters of the selected section spacing, and the video heading uses half that spacing below its separator. Introductory margins and line lengths remain unchanged. A 16ch headline measure balances the split layout without squeezing the first section. Selected font/logo sizes, button targets and spacing presets remain active.
- Section navigation starts with Home to the root route, then uses “October’s Book” for its book link and links to labelled visible blocks. Links follow page order, exclude hidden/removed sections, and support editable labels. In the editor, Home scrolls to the masthead without reloading the unsaved canvas. The larger-screen sticky bar has scroll offsets, keyboard focus targets and reduced-motion support. Below 900px, a Menu button opens a right-side drawer with native focus containment, Escape/backdrop dismissal and background scroll locking. In Edit page, menu words are editable and adjacent arrows jump to their destinations. Menu jumps do not outline or select the destination; explicit editor selection and interactive keyboard focus indicators remain available.
- Lower-page sections use full-width rows with headings beside their text on desktop, bounded text line lengths, and one rule below the FAQ heading; individual questions have no separators. The editor exposes this as “Heading beside text”; smaller screens stack the content. Opening, image and prose sections use a consistent 900px transition; zero-minimum grid tracks, long-text wrapping and adaptable dates protect narrower views and enlarged text.
- Editor offers Phone (up to 390px), Tablet (up to 768px) and available-width Desktop previews. Preview controls, dialogs and the editor toolbar can wrap on small screens; touch controls have larger minimum targets.
- All homepage text labels, menu labels, button wording, date labels, artwork captions and footer words are configurable under Labels & menu or directly on the canvas. Privacy and 404 copy are also configurable. Clicking either displayed date opens its calendar field. The masthead description is centered beneath the logo.
- A visual formatting toolbar supports selected bold/italic text and paragraph lists, with native keyboard shortcuts and clear formatting. Headings and short labels retain inline formatting; prose supports bulleted and numbered lists. Safe structured formatting is validated against canonical text and remains private until a preview is built. Plain inspector replacement clears that field’s formatting.
- Private local editing UI: text, dates, links, logo upload/reset and search listing, plus four page compositions, width/spacing/typography controls, custom colours with contrast validation, button shapes and Onegin artwork placement/size.
- Visual editing canvas renders the shared homepage components, with direct text editing, section move handles, drag-to-insert palette, insertion controls and arrow-button alternatives. Section options remain available in the inspector. The local canvas uses isolated per-tab snapshots and is excluded from production output.
- Up to 24 text, FAQ, image-and-text, button and quote blocks, with optional buttons in any block, duplication, visibility, width/alignment/background controls, text columns and image placement/crops. Undo/Redo tracks 40 changes in the current tab and groups inline typing; style reset preserves content and ordering. Unfinished blocks can be saved privately but must be completed or hidden before preparing a public preview.
- Draft save with stale-window conflict detection; exact saved previews; phone/desktop preview sizes; private upload staging; previous-content recovery mechanism.
- Public publishing adapter: validates a reviewed artifact, sends generated output to a dedicated branch, reconciles a pending retry and checks the live release marker. Disabled on this laptop until its publishing connection is ready.
- Editor publishing is the chosen route. Check publishing connection reads account, Pages, canonical domain, mode and environment prerequisites, shows setting links and enables the local adapter when ready. It does not modify GitHub or publish a release. The connection is checked again before publication; retrying the same successful release preserves Restore history.
- GitHub Actions workflow: checks source pushes/pull requests and deploys successful `main` builds after Pages activation. Optional editor mode deploys the exact reviewed `dist/` artifact from `site-live`; its push includes the workflow. Only one mode can publish, preventing stale source from overwriting editor content.
- Earlier Pages CMS field configuration retained as an inactive reference; it does not cover the expanded block/layout model. A Mac application launcher is supplied but has not been exercised yet.

## Checked

- Dependency installation completed; npm reported zero vulnerabilities at installation.
- Fifty-one automated tests passed, including isolated local HTTP/editor integration tests with real Astro preview builds for all four compositions and a check that editor pushes preserve the reviewed artifact while keeping the workflow outside public files. Background-drawing checks cover strict boolean settings, independent canvas snapshots and optimized assets in the saved preview.
- Tested: stale draft save, invalid URL/calendar/email inputs, image path restrictions, malformed upload type, rejected foreign origin/Host/token, missing image, artifact modification/symlink, private unselected upload, selected image rendering route, and disabled public publishing.
- Production build and built-page checks passed: confirmed copy/dates, canonical origin, working local references, no editor/private files in output and only the small navigation script in public output.
- AI overview checks cover hidden/unused-field exclusion, active membership links, custom section order, normalized video links, Markdown escaping and real editor previews using their reviewed draft. Built overview anchors resolve to the rendered sections.
- The local page returned HTTP 200 before its preview was opened.
- Tests also cover old-draft compatibility, exact block ordering, theme validation, strict setting types, image descriptions/dimensions, selected block image staging and readable preset colours. Generated HTML checks confirm that the book-left composition changes reading order along with its visual placement.
- Connection tests cover incomplete settings, unavailable credentials, branch versus tag permissions, approval rules and canonical URL redirects. HTTP tests check that the connection endpoint is protected and preserves the saved draft; recovery tests verify idempotent publication history.
- Canvas tests cover actual HTTP rendering, per-tab isolation, denied snapshot-file access, nonce/Host/Origin checks, stale-update rejection and unchanged saved revisions. Model and message-transport tests cover exact insertion/reordering, text-field allowlists, retained over-limit text, safe buttons, unfinished private drafts and text acknowledgement during inspector locks. Production preview builds after canvas startup remain free of development scripts and canvas controls.
- Source review identified and corrected private upload leakage, lock ownership, preview validation, artifact copying, pending publication retry, dark-theme skip-link contrast, narrow text columns and second-tab build-state recovery.
- Targeted browser checks on 13 September covered 320px and 390px phone layouts, a 768px tablet and a 1280px laptop without horizontal page overflow. Phone and tablet navigation use a right-side drawer; laptop navigation uses the horizontal bar. The How it works link was clicked and its video destination and border visually inspected. Formatting checks exercised bold, italic, bullets, clear formatting, save/reopen, menu editing and undo in the drawer, and clicking a date to focus its calendar field. Automated checks cover rich-text validation, safe markup, paragraph preservation and production previews. The approved content was restored after temporary formatting tests.

These checks include a targeted browser check of the opening. They are not a completed browser visual/accessibility audit, a successful public release, or owner acceptance.

## Still needed

- Recheck the publishing connection on this laptop, which currently reports disconnected, and verify Publish/Restore with Olga’s account. The initial source PR is merged and site-live contains publication commits. Current Pages and environment requirements are checked by the editor before it enables publication.
- Olga's real editing walkthrough, browser/phone/keyboard/zoom checks and native Mac launcher verification.
- Playback verification of the enabled club introduction in Saved preview. YouTube’s public oEmbed endpoint returned the expected video and player; production and integration checks verify its embed, menu link, position and AI overview. Actual playback has not been tested.
- An operational public email before showing Contact Olga; approved membership terms and Patreon destination before opening membership.
- Full homepage/reading-guide content and member-offer sections in the later planned release.
- A synchronization model if editing from multiple laptops or enabling hosted Pages CMS. The local prototype does not auto-commit private drafts or sync them through GitHub.

The private Sites deployment is a review copy. This update prepares source changes and a local saved preview; it does not publish a new public release or change DNS. No participant responses or private planning documents are included in this source repository or build.
